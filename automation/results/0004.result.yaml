# automation/results/0004.result.yaml
id: 0004
status: done
message: |
  env_check（S6 联调首单，ref et9lockStepYIUISeqFrameAniTestDev，已 pull --rebase 同步）。
  环境状态：
  - unity-cli 1.0.0-beta.6，installPath C:\Users\Liang\AppData\Local\Unity\bin\unity.exe；
    doctor 仅 check.binary-on-path=warn（CLI 未入 PATH，本次以全路径调用，白名单 Bash(unity *) 已就位）；
    auth.loggedIn=false（未登录 Hub，doctor 其余全 pass，退出码 0）。
  - 编辑器 6000.0.25f1 已装（doctor editor.14），与工程版本一致。
  - unity command 正常列出全部命令 = 编辑器在线、pipeline 已连接；
    命令清单完整，含 yiui_build_panel / yiui_build_all / yiui_preview_panel / eval / eval_file 等任务执行所需命令。
  - 本机 3 个 Unity 实例并存（pipeline 1 + GUI 2），沿用 0003 记录：Editor.log 共享，日志中旧错误轮次是中间态产物。
  - 代理：proxy.url http://127.0.0.1:7890/（doctor 显示，系统级）。
build:
  errors: []
log_tail: []
