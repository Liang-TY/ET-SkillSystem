# S7 失败路径测试：故意全错，且 prefabPath 指向已存在的 TestManyPanel.prefab——
# 预期：校验阶段失败、结构化错误清单回传、原 prefab 字节不变（事务性验证）
panel:
  name: BrokenOnPurpose
  pkg: TestBroken
  prefabPath: Packages/cn.etetet.lockstep/Assets/GameRes/YIUI/TestMany/Prefabs/TestManyPanel.prefab

nodes:
  - name: u_BadNode
    type: buttton
    props: { textx: 1, fontsize: big, color: red }
  - name: u_ComLoop
    type: loop_scroll_v
    props: { padding: 5 }

events:
  - { name: badEvent, target: NotExists }
