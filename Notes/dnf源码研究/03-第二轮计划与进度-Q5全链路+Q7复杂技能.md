# 第二轮计划与进度：Q5 按键到伤害全链路 + Q7 复杂技能案例

> 前置：第一轮（01/02 总结）已完成，本轮用它的术语/API 底子。
> 本文档 = 第二轮的执行计划 + 进度跟踪 + 问题记录。新会话可凭本文档 + 00-总览 接手。

---

## 0. 速查

- **目标**：①（Q5）把"按键盘 → 技能触发 → 动画 → 特效 → 伤害 → 受击反应"的**文件级全链路**打通（第一轮还有三个断点：输入绑定、资源常量映射、.skl/.atk 配置结构）；②（Q7）拆解 2-3 个**新原型**复杂技能（弹道/抓取/召唤变身），看不同形态技能怎么写。
- **产出**：`04-按键到伤害全链路-总结.md`、`05-复杂技能案例-总结.md`（含对我们技能系统设计的映射与建议）。
- **执行**：3 agent 并行（F 输入链路 / G 配置与资源绑定 / H 复杂技能案例）+ 主会话综合。

---

## 1. Q5 全链路：要打通的三个断点

第一轮已有时序框架（01 总结 §1.2）。本轮补：

| 断点 | 问题 | 线索 |
|------|------|------|
| ① 按键→技能触发 | 玩家按某键，到 `sq_IsEnterSkill(SKILL_ID)` 为真，中间经过什么？技能栏位→键盘键位的映射配置在哪个文件？`sq_IsEnterSkill` 和 `sq_IsUseSkill` 的确切区别？谁扣 MP、谁查 CD（引擎还是脚本）？ | 知识库 09-数据传输.md；pvf 的 skill lst（如 swordmanskill.lst）、clientonly/、etc/、ui/ 下找键位/技能栏配置 |
| ② 常量→资源映射 | `CUSTOM_ANI_TRIPLESLASH_BLADESPIRIT1 <- 262`（swordman_header.nut）这个数字索引，到底层 .ani 路径的映射表在哪个文件？`CUSTOM_ATTACK_INFO_*` → 攻击盒/伤害数据的绑定在哪？ | swordman_header.nut 上下文；character/swordman/ 目录；知识库 04-动画系统.md（getCustomAnimation/[etc motion]）、14-ACT脚本说明.md |
| ③ 配置文件结构 | .skl（技能数值表）是明文还是二进制？字段（CD/MP/等级数据）怎么排布？.atk（攻击信息）结构？skillmanskill.lst 内容是什么？ | xxd/file 看 skill/swordman/*.skl 头部；网络逆向文档 |

全链路产物形态：每步一行"环节 → 所在文件/系统 → 关键 API"的总表，能把一次"按 Z 键放技能"从键盘走到怪物扣血。

## 2. Q7 复杂技能：候选原型（agent H 按实际可读性挑选，至少 3 个不同原型）

| 原型 | 候选 | 要看什么 |
|------|------|---------|
| 弹道类 | 鬼剑 波动剑（wave.nut，load_state 里 state 24）| 弹道 PO 的创建/飞行/命中/销毁 |
| 抓取类 | 鬼剑"抓头"类/格斗系抓取（用 onAttack 返回受击对象） | 抓取判定→控制对方状态（STATE_HOLD）→演出→伤害 |
| 召唤/变身类 | 男法召唤、鬼剑觉醒、剑影系统 | 召唤物生成/技能替换（ReplaceSkill）怎么写 |
| （备选）浮空技 | ashenfork 上挑 | upForce 浮空参数的实战设置 |

每个技能产出：完整执行过程（触发/状态切换/动画/PO/伤害/受击反应参数/结束）+ 第一轮没见过的新模式。

## 3. Agent 分工

| Agent | 任务 | 主要读 |
|-------|------|--------|
| F 输入链路 | 断点①：按键绑定/技能触发判定的完整链路 | 知识库 09/01/16；pvf 键位/技能栏配置文件（自己搜：clientonly、etc、ui、skill lst） |
| G 配置与资源绑定 | 断点②③：常量→资源映射表 + .skl/.atk 结构 | swordman_header.nut、character/swordman/、知识库 04/14、xxd .skl/.atk、网络逆向 |
| H 复杂技能案例 | Q7：3 个新原型技能深读 | sqr/character/swordman|mage + sqr/passiveobject + shared_passive_object |

主会话：综合 F+G+H → 写 04/05 两篇总结 → 更新本文档进度 + 00 总览进度表。

## 4. 进度记录

| 任务 | 状态 | 日期 | 备注 |
|------|------|------|------|
| 本计划文档 | ✅ | 2026-08-14 | |
| Agent F 输入链路 | ✅ | 2026-08-14 | 链路全通：4层输入(物理键→OPTION_HOTKEY→槽位/指令串→脚本)；hotkeysystem.co/swordmanskill.lst 均为明文；MP/CD 由引擎在 sq_IsUseSkill 内做；普攻走引擎原生 STATE_ATTACK=8 非技能 |
| Agent G 配置与资源绑定 | ✅ | 2026-08-14 | 全链实证：CUSTOM_ANI_*→.chr[etc motion] 表(262→tripleslash_ bladespirit1.ani 吻合)；.skl/.chr/.atk/.ani 全是明文 #PVF_File 格式；.skl 数值区([level info] 列数+每级行)解码；**更正第一轮：攻击盒在 .ani 帧数据不在 .atk，.atk 只存命中反应属性(lift up/push aside/force hit stun)** |
| Agent H 复杂技能案例 | ✅ | 2026-08-14 | 三原型全拆：弹道三形态(链式生灭/插值平移/引擎托管一次性)、抓取双人同步(三查+appendage锁+受害者侧状态机)、召唤(Monster+PO握手+寄生AI)、变身(双appendage)；浮空实战值 upForce=10/300 |
| 主会话综合：04-全链路总结 | ✅ | 2026-08-14 | 04-按键到伤害全链路-总结.md |
| 主会话综合：05-复杂技能总结 | ✅ | 2026-08-14 | 05-复杂技能案例-总结.md |
| 更新 00 总览进度表 | ✅ | 2026-08-14 | |

状态：⬜未开始 / 🟡进行中 / ✅完成 / ⚠️阻塞

## 5. 问题记录（执行中遇到就加）

| # | 问题 | 原因 | 对策 | 状态 |
|---|------|------|------|------|
| | | | | |
