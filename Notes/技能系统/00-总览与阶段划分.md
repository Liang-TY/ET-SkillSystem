# 技能系统总览与阶段划分

> 本文件夹是"帧同步帧动画技能系统"的**分阶段执行手册**。
> 每个阶段是一个**可独立验证的里程碑**，新会话读完阶段文档 + 该阶段的代码参考就能执行。
> **文档自包含**：所有需要的信息都在各阶段文档里，不需要查阅其他外部文档。

---

## 系统目标

在 ET9 帧同步工程里，做一个 DNF/横版格斗风格的技能系统：玩家/怪物有 HP、能攻击、能受击、有 Buff、有投射物、能放技能。所有逻辑确定性（帧同步安全）。

---

## 四层架构（落地后的目标）

```
① lockstep（已有，极小更新）     LSInput 加 Target 字段 + NumericType 常量
② aabb + collision（碰撞基础层）   AABB 结构 + 相交检测 + 宽相遍历
③ skill（技能框架包，新增）        数值组件 / Buff / Cast / Bullet / Area / SkillLogic
④ SkillDefs（技能定义，新增）      每个技能一个类（C# 代码，不是配表）
```

依赖方向：④ → ③ → ② → ①

---

## 阶段划分（每个阶段一个文档）

| 阶段 | 文档 | 里程碑（验收） | 涉及层 |
|------|------|---------------|--------|
| **0** | `01-阶段0-前置确认.md` | 确认 aabb/collision 包存在 + API；建 skill 包骨架 | ② |
| **1** | `02-阶段1-数值组件与HP.md` | 玩家/怪物有 HP；Log 能打出 HP 变化 | ③ |
| **2** | `03-阶段2-受击盒与碰撞.md` | 怪物受击盒正确（可视化/Log）；玩家攻击盒碰它 → 命中检测通过 | ②③ |
| **3** | `04-阶段3-攻击扣血闭环.md` | 玩家按键攻击 → 怪物 HP 扣减（最小战斗闭环） | ①③ |
| **3.5** | `04b-阶段3.5-打击感框架.md` | 顿帧/硬直/帧事件/输入缓冲——DNF 手感的命脉 | ③ |
| **4** | `05-阶段4-Cast与SkillLogic.md` | 技能有生命周期（OnCast/OnUpdate/OnEnd）；一个完整技能能跑 | ③④ |
| **5** | `06-阶段5-Buff系统.md` | Buff 增减属性、Tick 伤害、到时移除（燃烧/眩晕） | ③④ |
| **6** | `07-阶段6-投射物与区域.md` | 弹道飞行+碰撞命中；区域效果持续 Tick（火圈） | ③④ |
| **7** | `08-阶段7-视图层.md` | 技能开始/命中/结束 播特效+动画；血条 UI | 视图 |

---

## 执行原则

1. **顺序执行**：阶段间有依赖（阶段3依赖阶段1的HP+阶段2的碰撞）。
2. **每阶段独立可验证**：完成后能在 Unity 里看到明确效果（Log/可视化/操作）。
3. **代码先于配置**：技能用 C# 写（SkillLogic），不用 Excel 配表。Buff 用 BuffDef 配置驱动。
4. **回滚安全**：所有状态存 LSEntity（`[MemoryPackable]`），SkillLogic 无状态，不用 lambda/delegate。
5. **每阶段文档自带代码参考**：照着写，不需要查阅其他文档。

---

## DNF 研究结论已融入

三轮 DNF 源码研究（`Notes/dnf源码研究/`）的结论已更新进各阶段文档：
- 阶段 2：受击盒采样公式已实证定论（/100 + DNF.z→我们.y + 面左镜像）
- 阶段 3.5：受击反应是攻击方配置驱动（damageAct/upForce/backForce/hitStunTime）；顿帧暂不实现（DNF 证实攻方不停帧）；调参参考值已给（upForce=10/300）；连段窗口=[cancelFrame,动画末]
- 阶段 4：SkillContext 完整 API 已补；CD 双机制（默认TryCast+特殊OnEnd）；SubState 字段（连段子状态）；GameContent 项目命名（不叫 SkillDefs）
- 阶段 6：Bullet 多段间隔命中 + 一次性攻击对象 + 霸体帧标记

**项目命名决策**：技能逻辑+数据放 `GameContent` 项目（不只技能，还有装备/怪物数据），编译成 DLL 通过 HybridCLR 热更。详见 `Notes/dnf源码研究/配置方案选型与DNF翻译管线.md` §11。

---

## 关键决策：视图层 Route B（状态标记 + 轮询 diff）

**不走事件总线**（`EventSystem.Publish`），因为 LSUpdate 在回滚重放时会重复执行，导致事件重复触发（特效乱飞/音效重播/甚至逻辑实体重复创建）。

**Route B 方案**：逻辑实体加状态标记字段（`JustStarted/JustHit/JustFinished/JustAdded/JustRemoved/LastHitstunTimer`），LSUpdate 设标记、下帧清；视图层 IUpdate 轮询标记做 diff 表现。完全确定性、回滚安全。

各阶段实现状态标记的位置：
- 阶段 3.5（打击感）：`LSCombatComponent.LastHitstunTimer`（上帧值，视图 diff 硬直进入/退出）。
- 阶段 4（Cast）：`LSCast.JustStarted/JustHit/JustFinished`。
- 阶段 5（Buff）：`LSBuff.JustAdded/JustRemoved`。
- 阶段 7（视图层）：所有表现基于轮询 diff，不订阅事件。

---

## 新会话怎么用这套文档

1. 从 `01-阶段0-前置确认.md` 开始，确认当前进度到哪个阶段。
2. 读该阶段文档 → 按代码参考写 → 验证里程碑 → 更新进度。
3. 完成一个阶段进下一个，不要跳。
4. 所有信息都在本文件夹的文档里，不需要查阅其他外部文档。

---

## 当前进度速查（2026-08-17）

| 阶段 | 状态 | 说明 |
|------|------|------|
| 阶段 0（前置确认） | ✅ 完成 | aabb/collision 包已确认，skill 包骨架已建 |
| 阶段 1（数值组件与HP） | ✅ 完成 | LSNumericComponent+NumericType+挂到玩家/怪物+ForbidMove 检查；编译通过（用户已确认） |
| 阶段 2（受击盒与碰撞） | ✅ 完成 | 受击盒采样/临时攻击盒/命中判定/面左镜像全验证通过（数值精确）；AABB 静态 Id 计数器已移除（帧同步确定性） |
| 阶段 3（攻击扣血闭环） | ✅ 完成 | 按键攻击→命中→扣血闭环全验证（每按一次恰扣一次 50）；附修阶段1 Numeric 五层映射 bug；本地双 fiber 日志翻倍属预期 |
| 阶段 3.5（打击感） | ✅ 完成 | 攻击帧驱动（判定帧=有 attackBoxes 的帧，多盒）+ 硬直/受击动画 + 输入缓冲/取消窗口 + 不连发全验证通过；附修视图层 imagePos 锚点漂移（02-坐标系 §2.1） |
| 阶段 4（Cast 与 SkillLogic） | ✅ 完成 | Cast/SkillLogic/SkillContext(struct)/SkillLoader 守门员全验证；**技能内容独立编译管线落地**（DotNet~ csproj + ET/Skill/Compile 菜单 + .bytes 资源加载 + 反射注册）；ET.Skill=第5热更程序集 |
| 阶段 5（Buff 系统） | ✅ 完成 | LSBuff + BuffDefinition([BuffId]) + Actions 效果层（ContentLoader 泛型+守门员）+ ApplyHit Actions 化全验证（燃烧 3tick/眩晕 950ms/CD） |
| 阶段 6（投射物与区域） | ✅ 完成（Area 延后） | LSBullet+BulletDefinition（第四类内容）+穿透弹全链路验证（出生→飞行→命中→1.5s 精确销毁）；多图集视图+绝对摆位公式（§2.1 悬案终结）；**Area（火圈）延后到有资源时做** |
| **阶段 7（视图层）** | **← 下一步** | 读 `08-阶段7-视图层.md` 开干（特效混合/血条/表现 polish） |
| 阶段 3.5 顿帧 | ⬜ | 按定论暂不实现 |

**已建代码**：`Packages/cn.etetet.skill/`（实体：Numeric/Hitbox/Combat/InputBuffer/Cast/Skill；ET.Skill 框架程序集：SkillLogic/SkillContext(struct)/SkillLoader(守门员)/SkillCastHelper/LSCastSystem/LSCastComponentSystem/LSNumericComponentSystem；Hotfix System：Hitbox/Skill/InputBuffer/Combat；技能内容 DotNet~/Skills 两技能 + ET.SkillContent.csproj + ET/Skill/Compile 菜单 + SkillContentLoader）+ loader 扩展（热更 5 件套：DllNames×2/CodeLoader 三分支）+ lockstep 修改（组件链/输入缓冲/按键 J+K/动画注册/视图锚点修复/YooAsset 收集配置）+ aabb（移除静态 Id）+ 翻译工具 `E:\Projects\cs\parse-img-ani\DnfConfigTranslation`

**延后待办（记档）**：
- **luban 迁移专题**：当前无 luban（excel 包是 ET 自带简易导出器）；etmaster 有完整设施（yiuiluban net10 + config 基类）；内容规模化时（约阶段 7 前）单独做，届时 attack/buff/monster 表 luban 化。详见 05 §5
- **Actions 效果层**：阶段 5 引入（ET7.2 skillSystemLession 模式——无状态节点+表组合；过程逻辑留 SkillLogic，效果组合走 Actions）。详见 05 §5

**编译修正记录**（ET 铁律，别再犯）：
1. `[MemoryPackable]` 需要加 `using MemoryPack;`
2. lockstep 和 skill 包的 Scripts/Model/Share 都编进同一个 ET.Model 程序集 → 同名类不能定义两次
3. Entity 类只放字段（ET0006），方法全放 System 扩展方法 + `[FriendOf]`
