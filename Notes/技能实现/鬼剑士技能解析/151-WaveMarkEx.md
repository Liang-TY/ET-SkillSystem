# 强化-波动刻印（WaveMarkEx）

> 技能ID 151 | 级别 E（TP 强化被动） | 可实现性 ⛔（=基础版 047 ⛔——印→增伤的资源转化链不变；TP 两路增量虽是纯时序参数（计数层可表达），但整体价值随基础技核心 ⛔） | 分析日期 2026-08-22 | 批次 E5

## 1. 基本信息（.skl 实测）

| 项 | 值 | 来源 |
|---|---|---|
| 中文名 | 强化 - 波动刻印 | `WaveMarkEx.skl [name]` |
| 英文名 | WaveMarkEx（skl 文件名；[name2]=`Wave seal Upgrade`） | 同上 |
| 职业 | 阿修罗（[growtype maximum level] `0 0 0 0 5 0`；[skill fitness growtype]=4） | 同上 |
| 学习等级 | 65（[required level range] 5）；前置：技能 47 波动刻印 Lv1 | 同上 |
| 最高等级 | 10（TP） | 同上 |
| TP 消耗 | 2/级 | 同上 |
| 类型 | passive（**skill class 0**；[feature skill type] 1） | 同上 [type] |
| 一句话效果 | 印持续时间 +2.2s/级、生成间隔 -1s/级 | 同上 [explain ex] |
| 基础技文档 | 047-WaveMark.md（⛔ 技能资源标记系统样本） | 本目录 |

## 2. 强化增量（对照 047-WaveMark.md）

### 2.1 数据表形态：整表镜像（python diff 实测 40 行 × 7 列 diff=0）

- static `500 30000 5 10000` 同值镜像；[level info] 逐值镜像（col2=10000 生成间隔恒 10s）。
- [special level up]（dungeon）：**`1 1 + 2200`（static[1] 印持续时间 +2200ms/级）+ `-1 2 - 1000`（col2 生成间隔 -1000ms/级，`-` 减法格式）**；pvp 两行全 0。
- [skill preloading image] 空节。

### 2.2 增量明细

| 增量项 | 数据源 | 每 TP 级 | TP10 满级 |
|---|---|---|---|
| 印持续时间（static[1]=30000） | `1 1 + 2200` | +2.2s | 30+22 = **52 s** |
| 生成间隔（col2=10000） | `-1 2 - 1000` | -1s | 10-10 = **0 s**（疑引擎钳制，存疑 1） |

`-` 格式步长 = 减法（E2 记过的"% 与 - 两种步长格式"再实证——本案 `%`/`+`/`-` 三格式同批集齐：147 用 `+`、149 用 `%` 负值、151 用 `-`）。

### 2.3 增量性质判定：纯时序参数档（但宿主 ⛔）

两路增量都是 047 判定"可表达层"的计数系统时序参数（印寿命/生成节奏，047 §5-1 已证数值键+定时可表达）；但技能核心价值（印→智力/增伤消费）在 047 的 ⛔ 面——TP 随整体挂起。

### 2.4 资源增量

**0 新增**（图标 SkillIcon.img 槽 240/241）。

## 3. 关联文件清单（每行实测）

| 层 | 文件 | 完整路径 | 状态 | 作用 |
|---|---|---|---|---|
| .skl | WaveMarkEx.skl | `E:\Projects\cs\dnforigin\pvf源码提取部分\pvf\skill\Swordman\WaveMarkEx.skl` | ✅（223 行全读） | 镜像表 + TP 增量 |
| lst 条目 | ID 151 | `…\pvf\skill\swordmanskill.lst` 389-390 行 | ✅ | — |
| 反向链接 | WaveMark.skl [feature skill index]=151 | 同目录 | ✅ 实测 | 双向互指 |
| 脚本 | —（无） | `…\sqr\character\swordman\`（grep wavemarkex 零命中） | ⛔ | 引擎消费 |
| 基础技文档 | 047-WaveMark.md | 本目录 | ✅ | 数值/缺口继承源 |

## 4. 实现方���增量

**零新内容件。** 基础版 047 §5 分层草案落地时，TP 即两个定时参数：

| 参数 | 基础版 | TP 并入后（TP10 满级定值） |
|---|---|---|
| 印寿命 | 30s | 52s |
| 生成间隔 | 10s | 0s → 钳 1s（或按"上限 5 印已封顶"语义维持节奏；实现侧建议钳制下限） |

## 5. 翻译工具适配（DnfConfigTranslation）

| 文件 | 不支持的节/字段/文件类型 | 建议 |
|---|---|---|
| WaveMarkEx.skl | `.skl` 无子命令（7 列 + `-` 格式步长 + 空 [skill preloading image] 节） | 随 skl 子命令；空预载节按"同名节取非空"规则（R7-E4 已记档） |

翻译缺口 1 条（.skl，常驻）。

## 6. 困难与简化

| DNF 原版行为 | 缺口/困难（§6.3 档位） | 简化建议 |
|---|---|---|
| 印寿命/间隔增减 | 技能等级系统（R6-C1）；时序层本身可表达 | 满级定值 |
| TP10 间隔归 0 | 引擎钳制未考证 | 实现侧钳 1s 下限 |
| pvp 不强化 | 无 PVP 分流 | 不做 |

## 8. 存疑与缺口上报

**未考证项**
1. TP10 时生成间隔 10-10=0s 的引擎行为（钳制值？瞬时补满 5 印？）——pvp 表 Lv41+ 段也有 136 300 平台值旁证"封顶"语义存在。
2. static[3]=10000 与 col2 同值冗余（047 §8-2 遗留，本档未新增证据）。

**缺口上报**：无新系统级缺口。

**给轮间经验**：**[special level up] 三格式集齐**——`%`（149，含负步长）/`+`（147）/`-`（151，减法）——skl 子命令的四元组格式字段需按枚举 + 符号步长双维度设计（本批收口性证据）。
