# 强化-鬼影步（GhostStepEx）

> 技能ID 147 | 级别 E（TP 强化被动） | 可实现性 ⛔（=基础版 018 ⛔——TP 解锁的"受击触发无敌"撞无敌帧 + 受击管线钩子双缺口，且基础三主干本就全空转） | 分析日期 2026-08-22 | 批次 E5

## 1. 基本信息（.skl 实测）

| 项 | 值 | 来源 |
|---|---|---|
| 中文名 | 强化 - 鬼影步 | `GhostStepEx.skl [name]` |
| 英文名 | GhostStepEx（skl 文件名；[name2]=`Shadow pace Upgrade`） | 同上 |
| 职业 | 鬼泣（[growtype maximum level] `0 0 5 0 0 0`；[skill fitness growtype]=2） | 同上 |
| 学习等级 | 65（[required level range] 5）；前置：技能 18 鬼影步 Lv1 | 同上 |
| 最高等级 | 10（TP） | 同上 |
| TP 消耗 | 2/级 | 同上 [special purchase cost] |
| 类型 | passive（skill class 3；[feature skill type] 1） | 同上 [type] |
| 一句话效果 | 鬼影步持续时间内受击时概率进入无敌：机率 +6%/级、无敌持续 +0.2s/级（explain 另有"无敌时间增加值 0.12 秒"一行无数据行） | 同上 [explain ex] + special level up |
| 基础技文档 | 018-GhostStep.md（⛔ 移速消费链/无敌帧/元素转换三主干全撞） | 本目录 |

## 2. 强化增量（对照 018-GhostStep.md）

### 2.1 数据表形态：整表镜像（python diff 实测 60 行 × 11 列 diff=0）

- static `500 300` = 基础同值镜像；[level info] 11 列逐值镜像。
- [special level up]（dungeon）：**`-1 8 + 6`（col8 受击无敌机率 +6%/级）+ `-1 9 + 200`（col9 受击无敌时长 +200ms/级）**；pvp 四行全 0（含 col0/col1 占位 0 行）。

### 2.2 关键发现：**TP 开启基础表的死配置行为**（0 值开关形态）

基础表 col8/col9（受击无敌机率/时长）**恒 0**（018 §1 已实测）——基础版"受击概率无敌"（explain 有文案、数据全 0）是被 0 值死配置关闭的**新行为开关**，TP 是唯一激活路径（+6%/级、+200ms/级从 0 起步）：TP10 → 机率 60%、无敌 2.0s。同 R7-E2 的 114"数据模型存在新行为但被 0 值死配置关闭"形态，**第二例（level 列版）**。

### 2.3 增量明细

| 增量项 | 数据源 | 每 TP 级 | TP10 |
|---|---|---|---|
| 受击无敌机率（col8，基础 0） | `-1 8 + 6` | +6% | 60% |
| 受击无敌持续时间（col9，基础 0） | `-1 9 + 200` | +200ms | 2.0s |
| （explain"无敌时间增加值 0.12 秒"） | **无四元组行** | — | 疑引擎固定/文案残留（存疑 1） |

### 2.4 资源增量

**0 新增**（TP 纯数据件；图标 SkillIcon.img 槽 224/225）。

## 3. 关联文件清单（每行实测）

| 层 | 文件 | 完整路径 | 状态 | 作用 |
|---|---|---|---|---|
| .skl | GhostStepEx.skl | `E:\Projects\cs\dnforigin\pvf源码提取部分\pvf\skill\Swordman\GhostStepEx.skl` | ✅（234 行全读） | 镜像表 + TP 增量 |
| lst 条目 | ID 147 | `…\pvf\skill\swordmanskill.lst` 385-386 行 | ✅ | — |
| 反向链接 | GhostStep.skl [feature skill index]=147 | 同目录 | ✅ 实测 | 双向互指 |
| 脚本 | —（无） | `…\sqr\character\swordman\`（grep ghoststepex 零命中） | ⛔ | 引擎消费（E1 结论） |
| 基础技文档 | 018-GhostStep.md | 本目录 | ✅ | 数值/缺口继承源 |

## 4. 实现方案增量

**零新内容件。** 基础版 018 §5 的演示壳（GhostStepBuff）落地时，TP 行为并入路径：

| 参数 | 基础版 | TP 并入后 |
|---|---|---|
| 受击无敌 | 不做（018 §7：无敌帧缺口 + 前冲状态机缺失） | 管线+无敌帧落地后：受击钩子 roll 60% → 2s 无敌 Buff（ForbidMove+免伤标记） |

⚠ 前置缺口比基础版更多一层：受击触发需要"受击伤害管线钩子"（R3-A15/R5-B3 第 6 消费方之后又一形态——**受击→自身无敌**），生效需要无敌帧系统——两缺口串联。

## 5. 翻译工具适配（DnfConfigTranslation）

| 文件 | 不支持的节/字段/文件类型 | 建议 |
|---|---|---|
| GhostStepEx.skl | `.skl` 无子命令（11 列宽表 + [special level up] `+` 格式步长） | 随 skl 子命令（E 批设计输入累计）；宽表手抄 §2.2 已列关键值 |

翻译缺口 1 条（.skl，常驻）。

## 6. 困难与简化

| DNF 原版行为 | 缺口/困难（§6.3 档位） | 简化建议 |
|---|---|---|
| 受击概率无敌（TP 解锁） | **无敌帧缺失（R1-A5）+ 受击管线钩子缺失（串联）** | ⛔；落地顺序：先管线后无敌 |
| 机率/时长 +6%/+0.2s | 技能等级系统（R6-C1） | 满级定值 60%/2s |
| explain 第三行 0.12s 无数据行 | 未考证 | 不实现 |
| pvp 不强化 | 无 PVP 分流 | 不做 |

## 8. 存疑与缺口上报

**未考证项**
1. explain ex"每提升1等级时，无敌时间增加值： 0.12秒"无对应四元组（疑：前冲无敌时间 col1 的引擎固定加成或文案残留）。
2. col8/col9 基础 0 值是官方原版设计（TP 解锁）还是 mod 死配置——若原版如此，"TP=行为开关"是 E 类新形态（见 §2.2）。
3. pvp 表 10 列数据的行折行排版异常（pvf 写出器换行错位，机读需按列数重组；pvp 无 TP 增量，不影响增量结论）。

**缺口上报**：**"受击→自身无敌"形态**并入受击伤害管线钩子消费方清单（第 6 消费方 003 之后的同族新形态：roll 结果不改伤害而是给自身挂无敌——管线设计时回调需支持"不改伤害返回"路径）。

**给轮间经验**：**018 勘误回填**——借本档 11 向量模板解出基础档未考证列：col1=1000 恒 → **前冲无敌时间 1.0s**（修正 018 §1 "static[1]=300=前冲无敌"的推断——300 疑 Throw1 施法动画 300ms；static[1] 语义应改判施法参数）；col2=移速 +11%→70%（×0.1）；col3-6=普攻第 1-4 击魔法攻击力、col7=前冲魔法攻击力（%）；col10=基本攻击力和技能攻击力 +10%→69%（×0.1）；col8/col9=受击无敌（本档 §2.2）。
