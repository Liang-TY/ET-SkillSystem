# 强化 - 鬼印珠（ReflectGuardEx）——上一代同名 TP（与 228 并存）

> 技能ID 160 | 级别 E | 可实现性 🔶（增量与 228 逐字节相同：多段次数 +3/Lv，Area Tick 路线纯配置；**本技能是"两代同名 TP 并存"唯一实证样本——生效版本是 228，本技为孤儿残留**，demo 不单独建学习入口） | 分析日期 2026-08-22 | 批次 E7

## 1. 基本信息

| 项 | 值 | 来源 |
|---|---|---|
| 中文名 | 强化 - 鬼印珠（**与 228 同名同图标**） | `skill\Swordman\ReflectGuardEx.skl` [name] |
| 英文名 | ReflectGuardEx（取 skl 文件名；[name2] 实测 `Ghost gate return Upgrade`——与 228 的 [name2] 相同） | 同上 |
| 职业 | 阿修罗（[skill fitness growtype]=4；[growtype maximum level] `0 0 0 0 2 0` → TP 上限 **2**） | 同上 |
| 学习等级 | **99**（[required level range] 5；对比 228 的 55） | 同上 |
| 前置 | **技能 1（格挡 Guard）Lv1**——指向基础技 002（鬼印珠，技能 2）之外的技能，疑上一代残留错误（228 的前置是 2 Lv5） | 同上 [pre required skill] |
| 最高等级 | [maximum level] 5（本代可学 2） | 同上 |
| 类型 | passive · 特性技（[feature skill type] 1）· skill class 0——**老式声明型 TP** | 同上 |
| TP 消耗 | 3 点/级（[special purchase cost] 3，与 228 同） | 同上 |
| 图标 | SkillIcon.img **268/269（与 228 完全同一格）** | 同上 [icon] |
| 一句话增量 | 鬼印珠多段攻击次数 +3 次/Lv（不加攻击力）——与 228 一字不差 | 同上 [explain ex] |

## 2. 强化增量（TP 表）与两代对比（本批核心专项）

### 2.1 增量声明（与 228 逐字节相同）

python diff 实证（三向对拍 `ReflectGuardEx.skl` vs `ReflectGuard.skl` vs `ReflectGuardExp.skl`）：

| 节 | 160 vs 基础 002 | 160 vs 228 |
|---|---|---|
| [static data] dungeon（`250 150 10 120`） | **逐字节相同** | **逐字节相同** |
| [level info] dungeon（70 行 × 9 列） | **逐字节相同** | **逐字节相同** |
| [special level up] dungeon（`2 2 + 3`） | （基础无此节） | **逐字节相同** |
| [special level up] pvp（`1 1 - 0` + `2 2 + 0`） | — | **逐字节相同** |
| [level info] pvp | **不同**（见 2.2） | **不同**（见 2.2） |

即：**两代 TP 的强化语义完全一致**——static[2]（多段攻击次数基准 10）每 TP 级 +3，pvp 零增量。228 文档对 002 的 static[2] 回填结论对 160 同样成立。

### 2.2 两代差异全表（四项元数据 + pvp 表）

| 项 | 160（上一代） | 228（现行，E4 批已析） | 解读 |
|---|---|---|---|
| [required level] | **99** | 55 | 99 级门槛属高等级上限年代（旧版本 TP 段）；228 是改版后降门槛的新一代 |
| [growtype maximum level] | `0 0 0 0 2 0`（上限 **2**） | `0 0 0 0 3 0`（上限 **3**） | 新代放宽 TP 上限 |
| [pre required skill] | **`1 1`（指向技能 1 格挡）** | `2 5`（指向基础技鬼印珠 Lv5） | 160 的前置链挂在格挡上——疑上一代表结构残留（E3-①双向链规律在 160 身上断裂：无基础技指向它） |
| [level info] pvp | **50 行旧表，数值高于现行**（Lv1 珠攻 9 vs 现行 6） | 71 行，与基础 002 逐字节相同 | 160 复制自**旧版本基础表**（当时基础 pvp 表 50 行且数值更高）；228 复制自现行表——**两代 skl 是基础技两个历史版本的快照** |
| 其余（name/icon/cost/feature type/class/static/dungeon 全部） | 相同 | 相同 | 同一技能的两代数据 |

### 2.3 生效判定（决定性证据）

**基础技 `ReflectGuard.skl` 的 [feature skill index] = 228**（实测）——基础技的功能强化链唯一指向 228；160 无任何 skl/nut 链指向（白名单六目录 grep `reflectguardex` 0 命中，引擎消费证据与 228 同为零）。

**结论**：228 为现行生效版本（低门槛/高上限/前置链正确/表与现行基础一致）；**160 是上一代同名 TP 的残留孤儿**——lst 未清理导致两代并存。两者增量语义相同，实际学习中（若游戏按 feature 链走）只暴露 228。

## 3. 关联文件清单（每行实测）

| 层 | 文件 | 完整路径 | 状态 | 作用 |
|---|---|---|---|---|
| .skl | ReflectGuardEx.skl | `E:\Projects\cs\dnforigin\pvf源码提取部分\pvf\skill\Swordman\ReflectGuardEx.skl` | ✅（234 行） | 上一代副本 + 相同增量 |
| 新代 .skl | ReflectGuardExp.skl | 同 skl 目录 | ✅ | 生效版本（E4 批 228 文档已全析） |
| 基础技 .skl | ReflectGuard.skl | 同上 | ✅ | diff 对拍 + [feature skill index]=228 实测 |
| 基础技文档 | 002-ReflectGuard.md | `Notes\技能实现\鬼剑士技能解析\` | ✅ | 增量挂接点 |
| 新代文档 | 228-ReflectGuardExp.md | 同上 | ✅ | 生效版本分析（§5 数值表直接复用） |
| 注册行 / nut / PO / .ani / .atk | — | — | ⛔ 无（与 228 同：复用 002 的 PO 20033/20049 链） | 纯数据被动 |

## 4. 资源需求

无自有资源（图标 268/269 与 228 同格；[durability decrease rate] 50 / [shake screen] 2 200 为随基础表复制的 skl 元数据）。**缺失 img：0 张。**

## 5. 实现方案草案

**不单独实现**——增量语义与 228 完全相同（多段次数 +3/Lv），实现归属 228/002 一并落地（228 §5 的 Area Tick 路线表直接适用：跳数 = 10 + 3×TpLevel，TP 上限取 228 的 3 而非本技的 2）。若按本技单独建入口，唯一差异是 TP 上限 2（10/13/16 跳）——无实现价值。

**两代并存的处理建议（给收尾总览）**：E 类清单中"同基础技多 TP"的其余 4 对（95/225、97/154、99/212、101/166）经本批判明**均非两代 TP**（Ex 是二觉替换主动技、Exp 才是 TP，见各文档 §8）——**鬼印珠 160/228 是全项目唯一"真·两代 TP 并存"**，收尾总览可单列一行说明，并在 TP 学习系统（R6-C1）落地时按 feature 链只暴露 228。

## 6. 翻译工具适配

| 文件 | 不支持的节/字段 | 建议 |
|---|---|---|
| ReflectGuardEx.skl | `.skl` 无子命令；[special level up] `+` 型 + pvp 双行零增量（与 228 同格式） | 同 228 处理；**skl 子命令需容忍"同目录同名族多文件"（Ex/Exp 两代）按 [required level]/[pre required skill] 区分版本** |

本技能翻译缺口 1 类（.skl）。

## 7. 困难与简化

| DNF 原版行为 | 缺口/困难 | 简化建议 |
|---|---|---|
| 两代同名 TP 并存（学习界面展示哪代） | 我们无 TP 学习系统（R6-C1） | 不复现并存——只实现 228 语义（增量相同，仅上限差 1） |
| 多段次数 +3/Lv | 与 228 同（Bullet 多段缺口族 / Area Tick 路线） | 随 002/228 一并（Area Tick：跳数×150ms） |
| TP 学习（3 点/级） | 技能等���系统缺失 | 常量 TpLevel |

## 8. 存疑与缺口上报

- **未考证**：①[pre required skill] `1 1`（指向格挡）是有意为之还是旧版残留（无旁证，倾向残留——新代 228 指向 2 Lv5 且基础 feature 链指向 228）；②99 级门槛的版本年代（疑 60 级上限扩展期）；③160 的 pvp 旧表数值高于现行的原因（pvp 平衡改版 nerf 的历史快照，推断）；④两代是否曾在同一客户端同时可学（lst 并存≠界面并存，未考证）。
- **给收尾总览的最终结论（"新旧 TP 并存"拼图）**：本批 5 对"同基础技双版本"样本全部判明——**95/97/99/101 与各自 Exp 不是两代 TP，而是"二觉替换主动技（Ex）vs TP 强化被动（Exp）"两种性质并存**（铁证：四个基础技 [feature skill index] 分别=225/154/212/166，无一指向 Ex；Ex 自身 [type]=active、二觉 30 级档、前置=基础技 Lv1~5）；240 是 239 的升级主动技（同族规律）；**唯一真两代 TP = 160/228**：增量声明/static/图标逐字节相同，门槛（99→55）/上限（2→3）/前置（挂格挡→挂基础技）/pvp 表（旧 50 行→现行 70 行）四差异 + feature 链指向 → **228 生效、160 孤儿残留**。
