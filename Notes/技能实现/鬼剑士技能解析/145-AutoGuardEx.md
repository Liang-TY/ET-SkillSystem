# 强化-自动格挡（AutoGuardEx）

> 技能ID 145 | 级别 E（TP 强化被动） | 可实现性 ⛔（=基础版 003 ⛔——反击攻击力/浮空力两路增量落在"格挡反击"机制上，宿主被受击伤害管线钩子阻断，强化随基础技生死） | 分析日期 2026-08-22 | 批次 E5

## 1. 基本信息（.skl 实测）

| 项 | 值 | 来源 |
|---|---|---|
| 中文名 | 强化 - 自动格挡 | `AutoGuardEx.skl [name]` |
| 英文名 | AutoGuardEx（skl 文件名；[name2]=`Auto Guard Upgrade`） | 同上 |
| 职业 | 剑魂 + 剑影（[growtype maximum level] `0 5 0 0 0 5`——剑魂/剑影各 5 级；[skill fitness growtype]=1） | 同上 |
| 学习等级 | 65（[required level range] 3）；前置：技能 3 自动格挡 Lv1 | 同上 |
| 最高等级 | 10（TP） | 同上 [maximum level] |
| TP 消耗 | 1/级（[special purchase cost]，本批唯一 1 档） | 同上 |
| 类型 | passive（skill class 1；[feature skill type] 1） | 同上 [type] |
| 一句话效果 | 格挡后对敌人进行反击并使敌人浮空——反击物理攻击力/浮空力各 +10%/级 | 同上 [explain ex] + special level up |
| 基础技文档 | 003-AutoGuard.md（⛔ 受击管线钩子第 6 消费方） | 本目录 |

## 2. 强化增量（对照 003-AutoGuard.md）

### 2.1 数据表形态：**列扩展变体**（本批 7 TP 中唯一非纯镜像）

- 基础表 [level info] **2 列**（施放机率/持续时间）；Ex 表 **4 列 × 20 行**——前 2 列逐值镜像（`5 120000`→`100 120000` 机率/时长），**追加 col2/col3 两列**：反击物理攻击力 100→311、浮空力 350（恒定）。
- static：dungeon 无；pvp `500`（与基础 pvp 同，语义未考证随基础档）。
- [special level up]（dungeon）：`-1 2 % 10`（col2 反击攻击力 +10%/级）+ `-1 3 % 10`（col3 浮空力 +10%/级）；pvp 两行全 0（决斗场不强化，本批 7/7 通例）。
- [level property] 4 向量全明（机率/时长/反击/浮空），基础档 2 列语义由本档补齐**反击参数的数值来源**（基础档反击是 001-Guard 家族的反击窗口，数值原在引擎/001 侧）。

### 2.2 增量明细

| 增量项 | 数据源 | 每 TP 级 | TP10 满级 |
|---|---|---|---|
| 反击物理攻击力（col2：100→311%） | `-1 2 % 10` | +10% | ×2 → 200→622% |
| 浮空力（col3=350 恒定） | `-1 3 % 10` | +10% | ×2 → 700 |
| 施放机率/持续时间（col0/col1） | — 无增量行 | 不变 | 5-100% / 120s |

### 2.3 增量性质判定：纯数值档（但宿主 ⛔）

两路增量全是乘加系数，无新行为/新判定/新资源；数值宿主是"格挡成功后的反击"——003 判定的"触发后=格挡全套（反击窗）"属于受击管线钩子阻断面。pvp 增量 0。

### 2.4 资源增量

**0 新 img / 0 新 .ani / 0 新 .atk / 0 新 .obj**（实测）——TP 纯数据件；图标共用 SkillIcon.img 槽 236/237（UI 层）。

## 3. 关联文件清单（每行实测）

| 层 | 文件 | 完整路径 | 状态 | 作用 |
|---|---|---|---|---|
| .skl | AutoGuardEx.skl | `E:\Projects\cs\dnforigin\pvf源码提取部分\pvf\skill\Swordman\AutoGuardEx.skl` | ✅（139 行全读） | 列扩展表 + TP 增量 |
| lst 条目 | ID 145 | `…\pvf\skill\swordmanskill.lst` 383-384 行 | ✅ | — |
| 反向链接 | AutoGuard.skl [feature skill index]=145 | 同目录 | ✅ 实测 | 基础↔强化互指（E3 结论第 11 例） |
| 脚本 | —（无） | `…\sqr\character\swordman\`（grep autoguardex 零命中；passive_skill case 表无 145） | ⛔ | TP 消费在引擎取数访问器族（E1 结论） |
| 基础技文档 | 003-AutoGuard.md | 本目录 | ✅ | 数值/缺口继承源 |

## 4. 实现方案增量（并入基础技草案）

**零新内容件、零新注册点。** 基础版按 003 §5 落地（管线钩子立项后）时，TP 增量即反击段参数：

| 参数 | 基础版（003/001 草案） | TP 并入后（建议直用 TP10 满级定值） |
|---|---|---|
| 反击攻击力 | 反击窗伤害（001 家族） | ×2（col2 ×(1+0.1×10)） |
| 反击浮空力 | —（基础档无此列，疑引擎缺省） | LaunchY 700（col3 350×2） |

无 TP/技能等级系统期间（R6-C1 记档）保持基础版数值不动；TP 系统后 `col2 ×(1+0.1×TPLv)` 两行公式。

## 5. 翻译工具适配（DnfConfigTranslation）

| 文件 | 不支持的节/字段/文件类型 | 建议 |
|---|---|---|
| AutoGuardEx.skl | `.skl` 无子命令（常驻）；[special level up] 四元组 | 随 skl 子命令立项（R7-E3 已记档的设计输入）；**新输入：Ex 表列数可多于基础表（列扩展型）**——解析按 Ex 自身列数声明读 |

翻译缺口 1 条（.skl 类型，常驻）。

## 6. 困难与简化

| DNF 原版行为 | 缺口/困难（§6.3 档位） | 简化建议 |
|---|---|---|
| 反击攻击力/浮空力 +10%/级 | 技能等级/TP 系统缺失（R6-C1）+ 宿主在**缺失档：受击管线钩子**（003） | 管线落地后固定满级值 |
| 决斗场不强化 | 无 PVP 分流 | 不做 |

## 8. 存疑与缺口上报

**未考证项**
1. pvp static `500` 语义（随基础档 003 §8-1 同疑）。
2. 基础档（2 列）运行时反击数值的来源（TP 前 = 001 家族反击窗固定值？本档 col2/col3 是 TP 后权威源）。
3. [growtype maximum level] 剑魂+剑影双 5 与 [skill fitness growtype] 1 单值的取舍（剑影 5 是否实际可学，未考证）。

**缺口上报**：无新系统级缺口（与基础版共用"受击管线钩子"+"技能等级系统"两条既有缺口）。

**给轮间经验**：TP 数据表第三形态——**列扩展型**（E1 的"整表镜像/static 槽覆写"之外）：Ex 表在基础列后追加新列，增量行可指向新增列。skl 子命令设计需按 Ex 自身列声明解析，不能按基础表列数截断。
