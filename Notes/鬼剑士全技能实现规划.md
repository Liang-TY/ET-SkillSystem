# 鬼剑士全技能实现规划

> 2026-08-27 创建。本文档是鬼剑士技能从"分析文档"到"游戏内实现"的施工规划。
> 新会话读本文档 → 了解现状/规则/流程 → 按批次实施 → 记录进度。
> 分析文档（240 个技能逐个解析）在 `Notes/技能实现/鬼剑士技能解析/`，是实现时的第一手参考。

---

## 1. 游戏现状

### 1.1 项目概况

基于 ET9 帧同步的 2D 像素网游（借鉴 DNF）。Unity 6000.0.25f1，UI 框架 YIUI。
开发机（本机）无 Unity，编译/构建走任务总线（automation/）由 Unity 机执行。

### 1.2 技能系统架构

```
技能内容层（DotNet~/，独立 dotnet 编译，Unity 永不编译）
  Skills/*.cs     技能逻辑（SkillLogic 子类，[SkillId] 注册）
  Actions/*.cs    效果节点（LSAction 子类，[ActionId] 注册）
  Buffs/*.cs      Buff 配置
  Bullets/*.cs    投射物配置
  Areas/*.cs      区域效果配置
  编译产物 ET.SkillContent.dll.bytes → YooAsset → Assembly.Load

技能框架层（Runtime/，ET.Skill 程序集，F6 编译）
  SkillLogic/SkillContext/SkillCastHelper  生命周期+门面
  LSAction/LSActionContext                  效果节点
  BuffDefinition/BulletDefinition/AreaDefinition  配置基类
  ContentLoader<TAttr,TBase>               泛型注册器+守门员

逻辑实体层（Scripts/Model/Share/，ET.Model 程序集）
  LSCast/LSCastComponent/LSSkillComponent
  LSNumeric/LSHitbox/LSCombat/LSInputBuffer
```

**关键约束**：
- 技能内容在 DotNet~ 目录，**Unity 不编译**——改了要手动 `dotnet build` → DLL 改名 .bytes
- 帧同步铁律：禁 float/double（用 TSMath/FP）、禁实例字段、禁 UnityEngine、禁反射
- 每个类一个文件

### 1.3 已实现的技能

| 技能 | SkillId | AnimId | 按键 | 状态 |
|---|---|---|---|---|
| 普攻三段 | NormalAttack | SwordmanAttack1-3 | J | ✅ |
| 地裂波动剑 | WaveSword | NormalWave(5) | I | ✅ |
| 火圈 | FireCircle | FireCircle(6)/FireCircleEnd(7) | O | ✅ |
| 浴血之怒 | BloodBoom | SwordmanBloodboom(16)+特效17-20 | U | ✅ |
| 波动爆发 | ~~ReleaseWave~~ | ~~21-41~~ | ~~Y~~ | ❌ 已删除 |

**已注册的 AnimId**：1-7（通用+火圈）、10-20（鬼剑士系列+浴血之怒特效）、42-48（怪物系列）。
波动爆发系列（21-41）已删除，AnimId 段空出。

### 1.4 技能分类（240 个总）

| 类 | 数量 | 说明 | 本次是否做 |
|---|---|---|---|
| A | 73 | 主动攻击技能（有动画、有伤害、玩家按键释放） | ✅ 做 |
| B | 33 | 自身增益/状态技能（开 buff、召唤阵法、持续效果） | ✅ 做 |
| C | 51 | 被动技能（属性增益，不需操作） | ❌ 暂缓（需属性修改系统） |
| D | 15 | 生活技能（分解/制作/开店） | ❌ 不做 |
| E | 68 | 强化被动（给已有技能加数值，DNF 的 TP 系统） | ❌ 暂缓 |

**本次目标**：A 类 73 个 + B 类 33 个 = **106 个技能**。

### 1.5 NPK/IMG 资源管线

NPK 按需加载管线已建好（`Notes/NPK按需加载架构方案.md`）：

```
NPK 原始文件 → 改名 .npk.bytes 放 Bundles/NPK/ → YooAsset 加载
  → NpkArchive.Mount（读索引，不解像素）
  → NpkMountManager.ReadByFilename（文件名反查→提取字节）
  → NpkImgParser.Parse（解析像素帧）
  → RectpackSharp 打图集 → Sprite.Create → 注册
```

**NPK 源目录**：`E:\Download\dnf客户端\70不悔客户端\ImagePacks2`
**项目存放**：`Packages/cn.etetet.lockstep/Bundles/NPK/*.npk.bytes`
**旧管线**：`Bundles/AnimRes/*.img.bytes`（手工提取的 IMG，作为 fallback 保留）

**NPK→IMG 映射规则**：
1. NPK 文件名 → 内部虚拟路径前缀（如 `sprite_character_swordman_effect.NPK` 含 `sprite/character/swordman/effect/*.img`）
2. 挂载时自动建文件名反查表（`katana9200b.img` → `sprite/character/swordman/equipment/weapon/katana/katana9200b.img`）
3. 运行时按文件名查找，不需要知道完整路径

**找不到 NPK 中的 IMG 时**：
1. 先检查 ImagePacks2 目录是否有对应 NPK（文件名关键词匹配）
2. 用 Python 脚本扫描 NPK 索引确认虚拟路径
3. 确实缺失的 → 告诉用户补充对应 NPK

### 1.6 动画翻译工具

DnfConfigTranslation（`E:\Projects\cs\parse-img-ani\DnfConfigTranslation`）：
- `ani` 子命令：.ani → JSON（AnimClipData 格式，含 frames[].image.path）
- `als` 子命令：.als → JSON（AnimOverlayConfig，特效叠加配置）

**当前问题**：翻译时 `[IMAGE]` 标签的路径被简化成纯文件名（如 `BantuAmazones.img`），丢失了 NPK 虚拟路径前缀。

**需要改造**（双版本兼容）：
- 旧版 JSON：`"path": "BantuAmazones.img"`（只有文件名，当前游戏的形态）
- 新版 JSON：`"path": "sprite/character/swordman/effect/BantuAmazones.img"`（完整虚拟路径）
- 运行时：NpkMountManager 的 `ReadByFilename`（文件名反查）兼容旧版，`Read`（完整路径）兼容新版
- 改造点：AniParser 保留 .ani 里的原始路径不简化，新版 JSON 写完整路径

## 2. 代码规则

### 2.1 ET 分析器红线（写码前置遵守）

| 红线 | 规则 |
|---|---|
| Entity 纯数据 | ET0006 不声明方法；ET0010 不声明委托 |
| 跨 Entity 引用 | ET0020 必须 EntityRef<T> |
| 友元访问 | ET0002 访问他人内部必须 [FriendOf] |
| 帧同步数值 | ET0023 LSEntity 禁 float/double（用 TSMath/FP） |
| 异步纪律 | ET0008/0009 ETTask 必须 await 或 .Coroutine() |
| 静态字段 | ET0015 必须标 [StaticField] |
| 分层 | ET0032 Model 只放 Entity；ET0005 Hotfix 只放静态类 |
| 热更安全 | ET0004 Hotfix 无非 const 实例字段 |
| 命名空间陷阱 | ET 子命名空间下裸 Object→ET.Object、Scene→ET.Scene，必须全限定或 using 别名 |

完整规则：`Notes/ET框架分析器规则详解.md` + `Packages/cn.etetet.harness/skills/et-code/references/et-code-rules.md`

### 2.2 提交规范

- **一行简短**：中文一句话（动作+对象+影响）
- **禁止附加自动签名**（Co-Authored-By 等 trailer）
- **禁止 git pull**（只用 --rebase）、**禁止 merge**
- 命令一律 `pwsh`（PowerShell 7）

### 2.3 调试开发流程

```
开发机（无 Unity）：
  写代码/spec → git push dev
  ↓
需要编译验证时：
  git checkout -B automation origin/automation
  写 automation/tasks/<id>.yaml → push → push → git checkout dev
  告诉用户："对 Unity 机从 AI 说：处理 origin/automation 待办 <id>"
  ↓
Unity 机（从 AI）：
  git fetch → 按 skill 执行 → result 回传 automation 分支
  ↓
开发机验收：
  git fetch origin automation → 读 result → 合并 wip → 更新进度文档
```

**热更 DLL 编译**（技能内容代码改了后必须做）：
1. 在 Unity 机或开发机 `cd Packages/cn.etetet.skill/DotNet~ && dotnet build`
2. 产物 `ET.SkillContent.dll` → 改名 `ET.SkillContent.dll.bytes`
3. 放到 `Packages/cn.etetet.skill/Bundles/SkillContent/`
4. Unity 刷新后 YooAsset 加载

## 3. 新会话冷启动指南

### 3.1 必读文档（按顺序）

1. **本文档**（鬼剑士全技能实现规划）—— 总览+现状+规则+分批计划
2. `Notes/技能实现/01-DNF技能实现方案.md` §0-§4 —— 技能系统架构+概念映射+bloodboom 实例
3. `Packages/cn.etetet.skill/CLAUDE.md` —— 技能包编写规范（程序集归属+铁律+模板）
4. `Notes/NPK按需加载架构方案.md` §14 —— 作用域规则配置化（资源加载怎么工作）
5. `CLAUDE.md`（项目根）—— 角色自检+规范路由
6. `automation/PROTOCOL.md` §快速上手 —— 任务总线操作流程

### 3.2 实现一个技能的完整流程

```
1. 读分析文档：Notes/技能实现/鬼剑士技能解析/<ID>-<名字>.md
   → 了解 DNF 侧逻辑、文件链、可实现性判定、困难点、简化建议

2. 检查资源：
   .ani 文件在哪 → DnfConfigTranslation 翻译成 JSON → 放 Bundles/AnimRes/
   .img 文件在哪 → 检查 NPK 里有没有 → 没有就找 ImagePacks2 补充

3. 注册 AnimId：
   AnimConfigRegistry.cs 加常量
   LSAnimClipRegistrar.cs 加注册行（读 JSON + Register）

4. 注册 SkillId：
   SkillIdAttribute.cs 加常量
   SkillIds 映射加 case

5. 写技能逻辑：
   DotNet~/Skills/<名字>Skill.cs（SkillLogic 子类）
   可能还需 DotNet~/Areas/、DotNet~/Bullets/、DotNet~/Actions/、DotNet~/Buffs/

6. 绑定按键：
   LSOperaComponentSystem.cs 加 KeyCode → button 映射

7. 编译热更 DLL：
   cd DotNet~ && dotnet build → DLL 改名 .bytes

8. 编译验证 + 测试：
   下 compile_check 任务单 → 从机验证 → Play 测试

9. 记录进度：
   更新本文档 §4 进度表
```

### 3.3 NPK/IMG 查找方法

```python
# Python 脚本：扫描所有 NPK 找指定 IMG
import struct

def xor_decrypt(data, key):
    return bytes(a ^ b for a, b in zip(data, key))

base = b'puchikon@neople dungeon and fighter '
key = bytearray(256); key[:len(base)] = base
pos = len(base)
while pos < 256:
    key[pos] = ord('D'); pos += 1
    if pos >= 256: break
    key[pos] = ord('N'); pos += 1
    if pos >= 256: break
    key[pos] = ord('F'); pos += 1
key[255] = 0

import os
npk_dir = 'Packages/cn.etetet.lockstep/Bundles/NPK'
search = '目标img名'.lower()  # 如 'hardattack'

for f in os.listdir(npk_dir):
    if not f.endswith('.npk.bytes'): continue
    data = open(os.path.join(npk_dir, f), 'rb').read()
    if data[:16] != b'NeoplePack_Bill\x00': continue
    count = struct.unpack('<i', data[16:20])[0]
    for i in range(count):
        off = 20 + i * 264
        name = xor_decrypt(data[off+8:off+8+256], bytes(key)).split(b'\x00')[0].decode('utf-8', errors='replace')
        if search in name.lower():
            print(f'{f}: {name}')
```

如果项目 NPK 目录找不到，去 `E:\Download\dnf客户端\70不悔客户端\ImagePacks2` 找对应 NPK：
- 技能动画 IMG 通常在 `sprite_character_swordman_effect*.NPK`
- 角色动画 IMG 在 `sprite_character_swordman_equipment_avatar_skin.NPK`
- 怪物 IMG 在 `sprite_monster_*.NPK`
- 通用特效在 `sprite_common_commoneffect_glow.NPK`

## 4. 分批计划（按学习等级排序）

### 已实现（5 个）

| 技能 | ID | 学习等级 | 按键 | AnimId | 状态 |
|---|---|---|---|---|---|
| 普攻三段 | - | 1 | J | 12-14 | ✅ |
| 地裂波动剑 | 20 | 15 | I | 5 | ✅ |
| 火圈 | 39 | 25 | O | 6-7 | ✅ |
| 浴血之怒 | 24 | 35 | U | 16-20 | ✅ |
| ~~波动爆发~~ | ~~32~~ | ~~25~~ | ~~Y~~ | ~~21-41~~ | ❌ 已删除 |

### 第 1 批：Lv1-10 基础攻击技（5 个）

| 技能 | ID | 学习等级 | 分析文档 | 难度预估 | 资源需求 |
|---|---|---|---|---|---|
| 鬼斩 | 5 | 1 | 005-HardAttack.md | 中 | HardAttack.ani + sm_body0000.img |
| 上挑 | 46 | 5 | 046-UpperSlash.md | 低 | UpperSlash.ani + sm_body0000.img |
| 三段斩 | 8 | 5 | 008-TripleSlash.md | 中 | TripleSlash.ani + sm_body0000.img |
| 连突刺 | 11 | 5 | 011-DashAttackMultiHit.md | 中 | DashAttackMultiHit.ani + sm_body0000.img |
| 银光落刃 | 16 | 10 | 016-AshenFork.md | 中 | AshenFork.ani + sm_body0000.img |

### 第 2 批：Lv10-20 中级攻击技（5 个）

| 技能 | ID | 学习等级 | 分析文档 | 难度预估 | 资源需求 |
|---|---|---|---|---|---|
| 空中连斩 | 17 | 10 | 017-JumpAttackMulti.md | 中 | JumpAttackMulti.ani |
| 崩山击 | 65 | 15 | 065-HopSmash.md | 中 | HopSmash.ani |
| 裂波斩 | 58 | 15 | 058-VaneSlash.md | 中 | VaneSlash.ani |
| 十字斩 | 64 | 15 | 064-GoreCross.md | 中 | GoreCross.ani |
| 里鬼剑术 | 67 | 10 | 067-WeaponCombo.md | 中 | WeaponCombo.ani |

### 第 3 批：Lv20-30（5 个）

| 技能 | ID | 学习等级 | 分析文档 | 难度预估 |
|---|---|---|---|---|
| 月光斩 | 77 | 20 | 077-MoonlightSlash.md | 中 |
| 邪光斩 | 50 | 20 | 050-GrandWave.md | 高（投射物） |
| 拔刀斩 | 9 | 25 | 009-MomentarySlash.md | 高（范围攻击） |
| 破军升龙击 | 68 | 25 | 068-ChargeCrash.md | 高（位移） |
| 怒气爆发 | 24 | 25 | 024-BloodBlast.md | 高（区域效果） |

### 第 4 批：Lv30-40（5 个）

| 技能 | ID | 学习等级 | 分析文档 | 难度预估 |
|---|---|---|---|---|
| 嗜魂之手 | 31 | 30 | 031-GrabBlastBlood.md | 高（抓取） |
| 冰刃波动剑 | 21 | 30 | 021-IceWave.md | 高（投射物+冰冻） |
| 爆炎波动剑 | 22 | 30 | 022-FireWave.md | 高（投射物） |
| 死亡墓碑 | 44 | 35 | 044-TombStoneRain.md | 高（持续区域） |
| 嗜魂封魔斩 | 79 | 35 | 079-BloodyRave.md | 高（持续吸附） |

### 第 5 批：Lv40+ 大招（5 个）

| 技能 | ID | 学习等级 | 分析文档 | 难度预估 |
|---|---|---|---|---|
| 鬼影闪 | 60 | 40 | 060-GhostStepSlash.md | 高（瞬移+斩击） |
| 猛龙断空斩 | 72 | 40 | 072-RapidMoveSlash.md | 高（多段位移） |
| 幻影剑舞 | 73 | 40 | 073-IllusionSlash.md | 高（多段连击） |
| 血气之刃 | 103 | 40 | 103-BloodSword.md | 高（突进+爆炸） |
| 崩山裂地斩 | 81 | 40 | 081-OutRageBreak.md | 高（大范围） |

### 第 6 批之后

按学习等级继续排列剩余技能（鬼连斩/流心系列/共鸣系列/冥炎系列/二觉系列等），
每批 5 个，直到 A 类 73 个 + B 类 33 个全部完成。
完整清单见 `Notes/技能实现/鬼剑士技能解析/_批次与进度.md`。

## 5. B 类技能说明

B 类是自身增益/状态技能，实现方式与 A 类不同：

| B 类子型 | 举例 | 实现方式 |
|---|---|---|
| Buff 技能 | 暴走(+力量-防御)、破极兵刃(+武器攻击) | 挂 BuffDefinition，修改 LSNumeric |
| 召唤阵法 | 刀魂之卡赞(范围力量buff)、冰霜之萨亚(范围冰冻) | 挂 AreaDefinition，持续效果 |
| 持续状态 | 血之狂暴(持续掉血换攻击)、波动刻印(积攒印记) | LSNumeric 持续修改 + 视图效果 |
| 位移技能 | 后跳、后跳斩 | 瞬移+动画 |
| 被动触发 | 逆转反击(受击时反击)、自动格挡(概率格挡) | 事件监听+条件触发 |

B 类技能需要在 A 类技能跑通后再做（依赖 Buff/Area 系统完善）。

## 6. 翻译工具改造

### 6.1 改造目标

DnfConfigTranslation 的 AniParser 保留 .ani 原始路径，产出双版本 JSON。

### 6.2 改造点

文件：`E:\Projects\cs\parse-img-ani\DnfConfigTranslation\Ani\AniParser.cs`
文件：`E:\Projects\cs\parse-img-ani\DnfConfigTranslation\Ani\AniModels.cs`

当前行为：
```
.ani 里：`Monster/Bantu/BantuAmazones.img`
JSON 里：`"path": "BantuAmazones.img"`  ← 简化了
```

改造后：
```
.ani 里：`Monster/Bantu/BantuAmazones.img`
新版 JSON：`"path": "sprite/monster/bantu/bantuamazones.img"`  ← 完整虚拟路径（小写+sprite前缀）
旧版兼容：`"pathLegacy": "BantuAmazones.img"`  ← 旧格式保留
```

规则：
- .ani 路径 → 拼上 `sprite/` 前缀 → 全小写 = NPK 虚拟路径
- 同时保留旧格式（纯文件名）在 pathLegacy 字段
- 运行时优先用 path（完整路径）查 NpkMountManager.Read，找不到用 pathLegacy 查 ReadByFilename

### 6.3 何时做

在第一个技能实现时一起做——翻译第一个技能的 .ani 时改 AniParser，后续所有技能自动受益。

## 7. 进度记录

- **08-27** 文档创建。现状：5 个技能已实现，106 个待做。分批计划已定（按学习等级，每批 5 个）。
- **08-27** NPK 按需加载管线已建好（A-E 步骤完成），资源收集已配置化（源类型+作用域规则）。

## 8. 问题 / 待办

- AT_Up.img 在 NPK 中找不到（火圈特效，走 .img.bytes fallback）
- 热更 DLL 需重新编译（删除了 ReleaseWave 技能代码）
- 翻译工具改造（AniParser 保留完整路径）尚未做
- B 类技能的 Buff/Area 系统待完善
- C/D/E 类暂缓（需属性修改系统/生活系统/TP系统）
